# Decision Log

## 2026-07-20 — 생활권 MVP는 동일 행정구역으로 정의한다

### 결정

같은 생활권 내 상급 아파트 추천의 첫 기준은 동일한 `region_id`다. 선택 연도의 취소되지 않은 실거래 평균 평단가가 현재 아파트보다 높은 단지를 평균 평단가 내림차순으로 최대 10개 제공한다.

### 이유

- 현재 보유 데이터만으로 재현 가능하고 테스트할 수 있는 경계다.
- 생활권의 주관적인 해석을 피하고 추천 결과의 근거를 설명할 수 있다.
- 향후 교통 시간, 학군, 사용자 지정 생활권 데이터가 준비되면 기준을 확장할 수 있다.

---

> 이 문서는 중요한 결정과 그 이유를 짧게 기록한다.

## 2026-07-20 — 역사적 격차 수준은 최소 0%, 최대 100% 백분위로 표현한다

### 결정

현재 격차가 관측 연도 중 가장 작으면 0%, 가장 크면 100%로 표시한다. 추천은 현재 급지만 입력받고 1급지·2급지 위까지만 제공한다.

### 이유

- 현재 격차가 과거 범위의 어디에 있는지 방향성까지 명확하게 이해할 수 있다.
- 개인별 대출 조건 없이 객관적인 가격 차이에 집중할 수 있다.

---

## 2026-07-20 — 급지는 해제 거래를 제외한 연평균 평단가 순위로 계산한다

### 결정

거래별 평단가는 `가격(원) × 3.305785 ÷ 전용면적(㎡)`로 계산한다. 해제 거래를 제외하고 지역별 연평균을 구한 뒤 최고 1급지부터 최저 10급지까지 배정한다.

### 이유

- 면적이 다른 거래를 동일 기준으로 비교할 수 있다.
- 실제로 유지된 계약만 가격 수준에 반영한다.
- 사용자가 이해할 수 있는 단일 객관 지표를 유지한다.

---

## 2026-07-20 — 수집 거래는 source key로 중복을 방지한다

### 결정

지역코드, 아파트 식별값, 계약일, 면적, 층, 가격을 조합한 source key를 거래의 수집 고유키로 사용한다. DB 중복뿐 아니라 같은 API 페이지 안의 중복도 저장 전에 제거한다.

### 이유

- 같은 지역과 계약월을 다시 수집해도 거래가 중복 생성되지 않는다.
- 외부 API가 동일 항목을 반복 반환해도 한 번만 저장할 수 있다.

---

## 2026-07-20 — 실거래 금액은 원 단위 정수로 저장한다

### 결정

국토교통부 API의 만원 단위 문자열에서 쉼표를 제거하고 10,000을 곱해 원 단위 `BIGINT`로 정규화한다.

### 이유

- 가격 계산에서 부동소수점 오차를 방지한다.
- API 표시 형식과 내부 계산 단위를 분리한다.

---

## 2026-07-20 — DB 스키마는 Flyway와 실제 PostGIS 통합 테스트로 관리한다

### 결정

- Backend가 Flyway 마이그레이션을 실행한다.
- Collector는 스키마를 변경하지 않고 기존 스키마만 사용한다.
- 지역 경계는 geometry, 아파트 위치는 geography 타입으로 저장한다.
- 마이그레이션은 PostGIS Testcontainers 통합 테스트로 검증한다.

### 이유

- 애플리케이션과 DB 스키마 버전을 함께 관리할 수 있다.
- H2로 검증할 수 없는 공간 타입과 인덱스를 실제 PostGIS에서 확인할 수 있다.
- 두 애플리케이션이 동시에 스키마를 변경하는 충돌을 방지한다.

---

## 2026-07-20 — 외부 API는 콘솔과 코드에서 이중으로 차단한다

### 결정

- 네이버 콘솔의 일·월 한도를 비용 발생을 막는 최종 차단선으로 사용한다.
- Geocoding은 백엔드 호출 예산과 중복 제거를 적용하고 예산 소진 시 호출을 중단한다.
- 프론트엔드 브라우저 제한은 보조 수단으로만 사용한다.
- 한도 상향보다 캐시와 호출 절감을 먼저 검토한다.

### 이유

- 코드 오류나 키 오용이 발생해도 콘솔 한도를 넘지 않게 한다.
- 브라우저 저장소 기반 제한은 초기화하거나 우회할 수 있어 비용을 보장하지 못한다.

---

## 2026-07-20 — 네이버 Maps는 무료 한도 안에서만 사용한다

### 결정

- Dynamic Map: 일 10,000건, 월 100,000건
- Geocoding: 일 1,000건, 월 20,000건
- 사용량 70%부터 이메일 알림
- 사용하지 않는 Maps API는 비활성화
- 사용자 승인 없이 유료 구간으로 한도를 높이지 않음

### 이유

- 개발 단계에서 예상하지 못한 API 요금 발생을 방지한다.
- Geocoding 결과를 PostGIS에 저장하면 낮은 호출 한도로도 운영할 수 있다.

---

## 2026-07-20 — TDD를 기본 개발 방식으로 사용한다

### 결정

신규 기능과 버그 수정은 `Red → Green → Refactor` 순서로 진행한다. 테스트를 먼저 작성하고, 실패를 확인한 뒤 최소 구현으로 통과시키고 리팩터링한다.

### 이유

- 급지와 가격 격차 같은 핵심 계산 규칙을 명확하게 검증할 수 있다.
- 데이터 수집과 공간 연산의 회귀 오류를 줄일 수 있다.
- 테스트 가능한 구조를 유지해 이후 기능 변경을 안전하게 만든다.

---

## 2026-07-20 — 문서 업데이트를 모든 작업의 완료 조건으로 둔다

### 결정

모든 코드 작업이 끝날 때 관련 문서를 반드시 업데이트한다. 세부 규칙은 `groundrule.md`에서 관리한다.

### 이유

- 코드와 문서가 서로 다른 상태가 되는 것을 방지한다.
- 다음 작업을 시작할 때 현재 구현 상태와 결정 이유를 바로 확인할 수 있다.

---

## 2026-07-20 — 갈아타기 알림은 웹 알림으로 제공한다

### 결정

갈아타기 조건 도달 알림은 브라우저 웹 알림으로 제공한다.

### 이유

- 별도의 모바일 앱 없이 데스크톱과 모바일 브라우저에서 받을 수 있다.
- 이메일이나 외부 메신저 계정 연동 없이 시작할 수 있다.

---

## 2026-07-20 — 급지 지도를 메인 화면으로 제공한다

### 결정

서비스의 첫 화면은 지역별 급지를 색상으로 보여주는 지도다. 지역을 선택하면 급지와 평균 평단가를 함께 보여준다.

### 이유

- 서비스의 핵심인 지역별 가격 수준을 가장 직관적으로 전달할 수 있다.
- 검색이나 복잡한 조건 입력 없이 전체 시장을 탐색할 수 있다.

---

## 2026-07-20 — 갈아타기 추천은 현재 급지만 입력받는다

### 결정

현재 급지를 기준으로 1급지 위와 2급지 위를 추천하고, 각 급지의 평균 평단가와 현재·과거 대비 격차를 보여준다.

개인별 대출, 보유 자금, 필요한 추가 자금은 계산하지 않는다.

### 이유

- 대출과 자금 상황은 사용자마다 달라 일관된 추천 기준으로 사용하기 어렵다.
- 객관적인 급지와 가격 격차를 제공하고 최종 판단은 사용자에게 맡긴다.

---

## 2026-07-20 — 갈아타기 알림과 생활권 추천을 구현 기능으로 포함한다

### 결정

- 목표 격차 조건에 도달하면 알려주는 갈아타기 알림을 제공한다.
- 같은 생활권 안에서 현재 아파트보다 높은 가격 수준의 아파트를 추천한다.

### 이유

- 사용자가 가격 격차를 계속 직접 확인해야 하는 불편을 줄인다.
- 생활권을 유지하면서 더 좋은 아파트로 이동하려는 수요를 지원한다.

---

## 2026-07-20 — 문서는 핵심 파일만 유지한다

### 결정

다음 문서만 유지한다.

- vision.md
- groundrule.md
- features.md
- architecture.md
- folder-structure.md
- database.md
- roadmap.md
- decision-log.md
- todo.md
- ideas.md

### 이유

- 지금은 포트폴리오용 문서가 필요하지 않다.
- 내가 자주 읽고 수정하기 쉬워야 한다.
- 상세 문서는 실제 개발 중 필요해질 때 추가한다.

---

## 2026-07-20 — ERD는 database.md에 포함한다

### 결정

ERD 전용 문서를 만들지 않고 `database.md`에 포함한다.

### 이유

- DB 관련 내용을 한 파일에서 확인할 수 있다.
- 문서 수를 불필요하게 늘리지 않는다.

---

## 2026-07-20 — 급지는 평단가만 사용한다

### 결정

지역 급지는 평당 가격만 기준으로 계산한다.

### 이유

- 기준이 단순하고 객관적이다.
- 사용자에게 설명하기 쉽다.
- 교통, 학군 등 주관적인 가중치를 섞지 않는다.

---

## 2026-07-20 — Collector와 API 서버를 분리한다

### 결정

국토교통부 데이터를 수집하는 Collector와 Spring Boot API 서버를 별도 애플리케이션으로 둔다.

### 이유

- 데이터 수집과 사용자 요청 처리는 역할이 다르다.
- 수집 실패가 API 서비스에 직접 영향을 주지 않게 한다.
- 각각 독립적으로 실행하고 배포할 수 있다.

---

## 2026-07-20 — ApartmentPriceSummary를 MVP에서 제외한다

### 결정

아파트별 가격 요약 테이블을 처음부터 만들지 않는다.

### 이유

- 초기에는 PostgreSQL 쿼리로 충분하다.
- 실제 성능 문제가 확인되기 전까지 조기 최적화를 하지 않는다.

---

## 2026-07-20 — 저평가 지역 기능은 보류한다

### 결정

MVP에 저평가 지역 추천 기능을 넣지 않는다.

### 이유

- 가격이 낮은 이유가 저평가인지 실제 열위인지 객관적으로 구분하기 어렵다.
- 우선 가격 격차와 과거 데이터를 보여주고 판단은 사용자에게 맡긴다.
